import {Language} from '../config';

export const parse = (language: Language, content: string) => {
	switch (language) {
		case 'css':
		case 'js':
		case 'tsx':
		case 'typescript':
		default:
	}
};
